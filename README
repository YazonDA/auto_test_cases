1. case_01 "github авторизация"
Cкрипт автоматически определяет дефолтный (для операционной системы) браузер. И производит запуск именно в нём.
Переменные скрипта прописаны в коде. Выносить во внешний config не стал (хотя, возможно).
Текущая версия поддерживает Firefox и Chrome. Проверялась только в Ubuntu.

2. case_02 "Пуск в windows"
Скрипт написан для экрана Ubuntu для разрешения 1920х1080.
Срабатывает и на других разрешениях (не всех). Но, обычно, подобные скрипты не делают универсальными для всех типов операционных систем и разрешений экранов (имхо).
AutoIT имеет больше возможностей относительно библиотеки pyautogui. И я знаю об этом не понаслышке. Но, к сожалению, версия AutoIT для linux-систем отсутствует.

3. case_03 "random-text-generator изменение текста"
Cкрипт автоматически определяет дефолтный (для операционной системы) браузер. И производит запуск именно в нём.
Переменные скрипта прописаны в коде. Выносить во внешний config не стал (хотя, возможно).
Текущая версия поддерживает Firefox и Chrome. Проверялась только в Ubuntu.
Обнаружена особенность:
- при работе в Chrome окно браузера закрывается в момент завершения модуля main;
- при работе в Firefox подобного не обнаружено.

4. 
